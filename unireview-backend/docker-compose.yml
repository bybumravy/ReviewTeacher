version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: unireview-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: unireview_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgrespassword
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres_data:
